\begin{figure}[t]
\centering
\begin{tikzpicture}[
    box/.style={draw, rounded corners=2pt, align=center, minimum width=2.8cm, minimum height=0.95cm},
    smallbox/.style={draw, rounded corners=2pt, align=center, minimum width=2.3cm, minimum height=0.85cm},
    arrow/.style={-Latex, thick}
]
\node[box] (input) {Aggregated weighted\\directed graph};
\node[smallbox, below left=1.1cm and 0.7cm of input] (lrta) {LR-TA seed\\cycle reduction\\+ add-back};
\node[smallbox, below right=1.1cm and 0.7cm of input] (wmsf) {WMSF-style seed\\remove--minimize\\--stabilize};
\node[box, below=2.5cm of input] (bestseed) {Best internal seed\\by backward weight};
\node[box, below=1.3cm of bestseed] (scc) {SCC scoring and\\top-$K$ selection};
\node[smallbox, below left=1.1cm and 0.7cm of scc] (destroy) {Destroy move\\reactivate heavy removed\\remove light active};
\node[smallbox, below right=1.1cm and 0.7cm of scc] (repair) {Repair move\\restricted local-ratio\\+ restricted add-back};
\node[box, below=2.5cm of scc] (accept) {Accept only if\\backward weight improves};
\node[box, below=1.2cm of accept] (output) {Final incumbent\\ordering};

\draw[arrow] (input) -- (lrta);
\draw[arrow] (input) -- (wmsf);
\draw[arrow] (lrta) -- (bestseed);
\draw[arrow] (wmsf) -- (bestseed);
\draw[arrow] (bestseed) -- (scc);
\draw[arrow] (scc) -- (destroy);
\draw[arrow] (scc) -- (repair);
\draw[arrow] (destroy) -- (accept);
\draw[arrow] (repair) -- (accept);
\draw[arrow] (accept) -- (output);
\draw[arrow] (accept.east) .. controls +(1.7,0) and +(1.7,0) .. node[right, align=center] {otherwise restore\\previous incumbent} (bestseed.east);
\end{tikzpicture}
\caption{Conceptual overview of the integrated framework. Two constructive seeds are evaluated first; IPSNS then applies incumbent-protected SCC-local destroy-and-repair moves to the better seed only when they improve the global objective.}
\label{fig:framework-overview}
\end{figure}
