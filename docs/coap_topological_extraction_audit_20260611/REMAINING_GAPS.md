# Remaining gaps

1. **OR1 artifact ZIP** not regenerated after S02/S06 edits — rebuild before submission if supplement is bundled.
2. **EXP11** on IPSNS final active DAGs not run separately (LR-TA only); expected same extraction gap=0 pattern on nonnegative set.
3. **Literature register** partial — full-text verification limited to DF03 and ELS93 metadata; extend only if new citations added.
4. **Remote CI** still pending from prior audit.
5. **Cover letter** and COAP upload remain next submission steps.

No further experiments required for extraction concern on nonnegative sparse headline.
