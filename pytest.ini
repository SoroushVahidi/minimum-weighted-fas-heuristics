[pytest]
testpaths = tests
pythonpath = src
addopts = -q --strict-markers
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
norecursedirs = experiments/exp4_external_baselines/external_tools .git __pycache__
